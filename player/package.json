{
  "name": "carteleria-digital-player",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "vite preview --host --port 5500"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "vite": "^5.4.0"
  }
}
